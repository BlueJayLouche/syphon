//! Metal-based Syphon Sender - Renders and publishes actual frames
//!
//! This example uses Metal directly to render an animated test pattern
//! and publishes it via Syphon so you can view it in any Syphon client.
//!
//! NOTE: This example uses the full_metal_sender implementation.
//! Run `cargo run --example full_metal_sender` instead.

fn main() {
    eprintln!("This example has been replaced by 'full_metal_sender'.");
    eprintln!("Run: cargo run --example full_metal_sender");
    std::process::exit(1);
}
